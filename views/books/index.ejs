<h1>Books</h1>

<% books.forEach(function(book) { %>
  <div>
    <h2>
      <a href="/books/<%= book.id %>">
        <%= book.title %>
      </a>
      <%= book.authorName %>
    </h2>
  </div>
<% }) %>